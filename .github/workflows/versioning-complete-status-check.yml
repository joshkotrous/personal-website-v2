name: versioning-complete-status-check

on:
  workflow_run:
    workflows: ["increment-version"]
    types:
      - completed

jobs:
  Versioning_Complete:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Versioning Complete"
